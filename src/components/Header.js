import React from "react";

const Header =() => {
    return (
      <nav style={{ 
        backgroundColor: "red"
       }}>
        <h1>Header</h1>
      </nav>
    );
  };

  export default Header;